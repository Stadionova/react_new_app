import React from "react";
import classes from "./Friends.module.css";

const Friends = () => {
    return (
        <div className={classes.settings}>Friends Page</div>
    )
};

export default Friends;